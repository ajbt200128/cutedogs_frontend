import { DogPointsPipe } from './dog-points.pipe';

describe('DogPointsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DogPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
