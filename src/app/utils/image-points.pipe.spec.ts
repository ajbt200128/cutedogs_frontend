import { ImagePointsPipe } from './image-points.pipe';

describe('ImagePointsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePointsPipe();
    expect(pipe).toBeTruthy();
  });
});
